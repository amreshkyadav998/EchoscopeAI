# Auth Service (:8001)

User registration, login, JWT issuance/refresh, token blacklisting, RBAC (Admin/Analyst/Viewer),
OAuth2 social login, and TOTP 2FA. Owns the `users` and `organizations` tables.

_Implemented in Phase 3._
