# auth-service dependencies.
# Shared utilities (logger, config, exceptions, schemas):
-e ../common
# Central DB models (Organization, User, ...); migrations live in db/:
-e ../db

# Web framework + server
fastapi>=0.110
uvicorn[standard]>=0.29

# Database driver (async SQLAlchemy comes via echoscope_db)
asyncpg>=0.29

# Auth: password hashing, JWT, email validation
bcrypt>=4.1
pyjwt>=2.8
email-validator>=2.1

# Redis (refresh tokens + token blacklist)
redis>=5.0
