# common/

Shared Python utilities imported by all backend services: JSON logger (loguru), base exception
hierarchy, Pydantic base models, correlation-ID context var, UUID utils, and a config base class.

_Implemented in Phase 1 (Step 1.3)._
