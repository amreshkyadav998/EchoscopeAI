# DB tooling (migrations + seed). Copy to .env for host use.
# In Docker the host is "postgres" on 5432; from the host use 127.0.0.1 + the
# published port (5433 on this machine, since native PG holds 5432).
DB_URL=postgresql+asyncpg://echoscope:echoscope@127.0.0.1:5433/echoscope
