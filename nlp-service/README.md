# NLP / AI Service (:8003)

Kafka consumer on `mention-created`. Sentiment analysis (RoBERTa), spaCy NER, keyword
extraction (TF-IDF + KeyBERT), GPT-4o summarisation. Publishes `sentiment-processed`.

_Implemented in Phase 7._
