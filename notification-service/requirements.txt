# notification-service dependencies.
-e ../common
-e ../db
-e ../kafka
-e ../rpc          # gRPC client (Phase 12)

# Web framework + server (with WebSocket support)
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0

# DB (async SQLAlchemy comes via echoscope_db) + cache/pub-sub
asyncpg>=0.29
redis>=5.0

# JWT validation on the WS handshake
pyjwt>=2.8

# Alert email (Phase 10): HTML templating + SendGrid HTTP API
jinja2>=3.1
httpx>=0.27
